services:
  prismdoc:
    build: .
    ports:
      - "8000:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      PRISMDOC_CONFIG: ${PRISMDOC_CONFIG:-/app/examples/retail/pipeline.yaml}
